name: tka-production

services:
  backend-blue:
    extends:
      file: docker-compose.prod.yml
      service: backend
    image: ${RELEASE_IMAGE:?set immutable RELEASE_IMAGE}
    environment:
      APP_RELEASE: ${RELEASE_TAG:?set RELEASE_TAG}

  backend-green:
    extends:
      file: docker-compose.prod.yml
      service: backend
    image: ${RELEASE_IMAGE:?set immutable RELEASE_IMAGE}
    environment:
      APP_RELEASE: ${RELEASE_TAG:?set RELEASE_TAG}
